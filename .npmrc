# Install exactly what's in package-lock.json; fail if out of sync.
# (npm ci is preferred in CI, but this keeps local installs honest.)
save-exact=true

# Surface known vulnerabilities in transitive deps at install time.
audit-level=moderate

# Require the engines (Node version) declared in package.json.
engine-strict=true
